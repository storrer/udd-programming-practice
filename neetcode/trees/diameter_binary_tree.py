from typing import Optional


class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right


class Solution:
    def diameterOfBinaryTree(self, root: Optional[TreeNode]) -> int:
        if root == None:
            return 0
        return 1


y = [1, 2, 3, 4]


def superduper(
    q: int,
    fun: list[int],
    typist: tuple | dict,
):
    return "hello"


superduper(12, [3], {x: x**2 for x in y})
